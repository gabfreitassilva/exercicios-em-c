#include <stdio.h>

int main() {
    float num;
    printf("Digite um numero com casas decimais: ");
    scanf("%f", &num);

    printf("Valor lido: %d", num);
    // checar o erro no terminal para entender mais
    // dica de estudo.: tipos de entrada e saida de dados
    return 0;
}