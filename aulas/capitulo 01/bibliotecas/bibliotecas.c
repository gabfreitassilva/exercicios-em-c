#include <stdio.h> // o comando #include e usado para incluir a biblioteca que vamos utilizar

int main() {

	printf("Quando adicionamos uma biblioteca, devemos colocar entre o par de < (menor que) \n> (maior que), mas se for uma biblioteca local, que voce mesmo criou e esta na \nsua maquina, devemos colocar entre um par de \" (aspas) \"\n.");

	return 0;
}
