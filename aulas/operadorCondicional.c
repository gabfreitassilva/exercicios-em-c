#include <stdio.h>
int main() {
    int primeiroNumero = 10, segundoNumero = 10, calculo, calcul;
    
    /* printf("Digite um número: ");
    scanf("%i", &primeiroNumero);
    printf("Digite outro número: ");
    scanf("%i", &segundoNumero);
    */

     primeiroNumero < segundoNumero ? printf("sim\n") : printf("não\n");

    primeiroNumero < segundoNumero ? calculo = 20 : printf("não");
    

}